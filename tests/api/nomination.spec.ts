import { test, expect } from '@playwright/test';

test.describe('Nominations', () => {
    test('should get all candidates nominated for a position', async ({ request }) => {
        // TODO
    })
});
